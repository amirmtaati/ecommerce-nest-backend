type Product {
  title: String!
  desc:  String
  price: Int!
}

type Query {
  products: [Product]
}

type Mutation {
  createProduct(title: String! , desc: String! , price: Int!): Product
}

type Mutation {
  deleteProduct(id: String!): Product
}

